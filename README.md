# AI-ROAD-POTHOLE-DETECTOR-
Pothole Detector is a Flask-based web app that uses a YOLO ONNX model to detect road potholes from camera, video, or uploaded images. It runs on web (PC and mobile), shows pothole counts and locations on a map, keeps a local history, and can generate a formatted report for complaints
